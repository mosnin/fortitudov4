import type { ShaderVariantId } from "@/components/shader/lib/shader-variants";

export const features = {
  smoothScroll: true,
} as const;

export const SHADER_VARIANT_DEFAULT: ShaderVariantId = "fortitudo";
